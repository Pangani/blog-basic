<h1>The Show Article Page</h1>

<p>
    <strong>Title</strong>
    <%= @article.title %>
</p>
<p>
    <strong>Text</strong>
    <%= @article.text %>
</p>

<h2>Comments</h2>
<% @article.comments.each do |comment| %>
    <%= render @article.comments %>
<% end %>


<i class="fas fa-h2">Add a Comment</i><br>

    <%= render 'comments/form' %>

<%= link_to "Edit", edit_article_path(@article) %> |
<%= link_to "Back", articles_path %>